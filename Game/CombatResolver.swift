import Foundation

/// CombatResolver — combat logic has been migrated into GameState.
/// This file exists to satisfy the Xcode project reference.
@MainActor
final class CombatResolver {
    // Deprecated: all combat resolution (attack, spell, hack, blitz, etc.)
    // is now handled directly in GameState.swift.
}
